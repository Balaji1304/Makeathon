DB_HOST=localhost
DB_PORT=5432
DB_NAME=greentrack
DB_USER=greentrack_user
DB_PASSWORD=securepass
DB_ECHO=false

# Optional: postgres URL for one-time schema fix (permission denied for schema public).
# If set, init-db will use it to grant public schema to DB_USER then create tables.
# Example: postgresql://postgres:postgres@localhost:5432/greentrack
# ADMIN_DATABASE_URL=

# Analytics cost model (defaults are reasonable demo values)
# COST_PER_KM=1.5
# DRIVER_COST_PER_MIN=0.5
